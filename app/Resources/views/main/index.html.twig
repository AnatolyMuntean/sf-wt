{% extends "base.html.twig" %}

{% block body %}
    <div class="row">
        {% for vendor in vendors %}
            <div class="col-sm-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title">{{ vendor.country }} <a href="#">(tech tree)</a></h3>
                    </div>
                    <div class="panel-body">
                        <div class="row">
                            {% for vehicle_type in vehicle_types %}
                            <div class="col-sm-3">
                                <div class="panel panel-default">
                                    <div class="panel-heading">
                                        <h3 class="panel-title">{{ vehicle_type.name }}</h3>
                                    </div>
                                    <div class="panel-body">
                                        <ul>
                                            {% for tank in tanks %}
                                                {% if tank.vendor.id == vendor.id and tank.type.name == vehicle_type.name %}
                                                    <li>
                                                        <a href="{{ path('tank_page', {'tank': tank.id}) }}">{{ tank.name }}</a>
                                                    </li>
                                                {% endif %}
                                            {% endfor %}
                                        </ul>
                                    </div>
                                </div>
                            </div>
                            {% endfor %}
                        </div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
